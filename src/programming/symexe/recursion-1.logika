import org.sireum.logika._

def multRec(m: Z, n: Z): Z = {
  l"""{ requires n ≥ 0
        ensures  result == m * n }"""

  // Start

  var r: Z = 0
  if (n != 0) {
    r = multRec(m, n - 1)
    r = m + r
  }

  // End

  return r
}
